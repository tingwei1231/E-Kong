# ── Core Web Framework ──────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
httpx==0.27.0

# ── LINE Messaging API ───────────────────────────────────────────────────────
line-bot-sdk==3.11.0

# ── Tunnel / ngrok ───────────────────────────────────────────────────────────
pyngrok==7.1.6

# ── STT: Groq Whisper API（取代本地 faster-whisper）─────────────────────────
groq>=0.9.0

# ── LLM: llama-cpp-python (GGUF 4-bit, CUDA offload) ────────────────────────
# setup_colab.py 會以 --extra-index-url 覆蓋安裝 CUDA 預編譯版本
# 本行僅作為 CPU fallback（本機開發 / CI 環境）
llama-cpp-python==0.2.90

# ── TTS 已移除（架構改為純文字輸出） ─────────────────────────────────────────
# chattts, transformers, soundfile 已不再需要

# ── ffmpeg 依賴（音訊轉換 m4a → WAV）────────────────────────────────────────
# Colab 已預裝；本機需手動安裝 ffmpeg binary

# ── Observability & Utilities ────────────────────────────────────────────────
loguru==0.7.2
pydantic==2.7.1
pydantic-settings==2.3.0

# ── Dev / Test（本機開發用，Colab 可選裝）────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
